1. ClientConnectionImpl::driveRead(SocketMultiEventListener& listener)에서 input이 pending되있으면 한 client로부터 1MB 이상의 요청이 오게 되면 그걸 받느라고 다른 client의 요청을 처리하지 못 함. context switching처럼 우선은 다른 client의 요청을 처리하고 다시 돌아와서 read를 마저하는 게 좋지 않을까요? 이 부분에 대해서 검토 바람. 특정 길이(PDU) 사이즈까지는 무조건 이 클라이언트가 점유해서 받는 방식도 있을 것 같은데 실제적으로 속도에 영향이 있는지 모르겠음.
2. 
